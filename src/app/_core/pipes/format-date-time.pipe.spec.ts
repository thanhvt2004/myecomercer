import { FormatTimeStampUnixToDatePipe } from './format-date-time.pipe';

describe('FormatTimeStampUnixToDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTimeStampUnixToDatePipe();
    expect(pipe).toBeTruthy();
  });
});
